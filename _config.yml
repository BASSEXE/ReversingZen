# Site Build Settings

# jekyll-paginate configuration force update
paginate:            4

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

sass:
  sass_dir: assets/css
  style: :nested

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "X3ASSEXE RESEARCH JNL"
description:	       "GREY TEAM NOTES - " IT's NOT ALL BLACK AND WHITE"
author:             "X3ASSEXE the GREY"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
